/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    // 디자인 토큰(색·폰트·간격)은 디자이너와 확정 후 여기 extend 에 추가
    extend: {},
  },
  plugins: [],
}
